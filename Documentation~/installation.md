# Installation

For local development, add `package.json` with Unity Package Manager's **Add package from disk** action. After publication, install the repository or a version tag with **Add package from git URL**.

The package name is `com.baran.apple-foundation-models` and the minimum declared Unity version is 2022.3.
